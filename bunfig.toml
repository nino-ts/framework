# Bun Configuration for Ninots Framework
# https://bun.sh/docs/runtime/bunfig

[install]
linker = "hoisted"
linkWorkspacePackages = true

[install.scopes]
"@ninots" = "https://registry.npmjs.org/"

[test]
coverage = true
coverageThreshold = { line = 80, function = 80, branch = 70 }
coverageReporter = ["lcov"]